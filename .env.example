# Local/Compose defaults (safe; no secrets)
MONGODB_URI=mongodb://mongo:27017/chainlite
MONGODB_DB_NAME=chainlite
HOST=0.0.0.0
PORT=8000

# For development without Docker, you may point to local MongoDB
# MONGODB_URI=mongodb://localhost:27017/chainlite

# For production: DO NOT commit secrets. Provide secure values via CI/CD or env injection.
# Example (Atlas):
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-address>/chainlite?retryWrites=true&w=majority
